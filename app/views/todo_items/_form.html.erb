<div id="error_explanation">
				<h2><%= pluralize(@todo_item).errors.count, "error") %> prohibited this
					todo_list from being saved</h2>

			<ul>
			<% @toto_list.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
			<% end %>
			</ul>
		</div>

	<%= form.label :content %>
	<%= form.text_field :content %>

	<%= form.submit "Save" %>